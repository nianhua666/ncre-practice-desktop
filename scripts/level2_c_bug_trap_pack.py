from __future__ import annotations


def _tag(question, topic: str, frequency: str = "high") -> dict:
    question["topic"] = topic
    question["frequency"] = frequency
    question["source"] = "curated"
    return question


def build_level2_c_bug_trap_pack(single_choice, fill_blank, code_completion, subjective, rubric_item):
    return [
        _tag(single_choice("L2C-151", "若 `char s[10]; scanf(\"%9s\", s);`，这里格式串中数字 9 的主要作用是：", ["A. 只允许输入 9 个数字", "B. 限制最多读取 9 个非空白字符，避免越界", "C. 让字符串长度固定为 9", "D. 自动补全结束符"], "B", "预留一个位置给结束符，是常见的安全输入写法。", ["高频", "字符串", "安全输入"], "medium"), "字符串"),
        _tag(single_choice("L2C-152", "关于 `fgets(buf, n, fp)` 的说法中，正确的是：", ["A. 一定不会把换行符读入缓冲区", "B. 最多读取 n 个有效字符", "C. 最多读取 n-1 个字符并补结束符", "D. 读取失败时返回 0 而不是指针"], "C", "fgets 需要为结束符保留空间。", ["高频", "文件", "fgets"], "medium"), "文件"),
        _tag(single_choice("L2C-153", "若 `int *p = a;`，则表达式 `p[i]` 与下列哪一项等价？", ["A. `*(p+i)`", "B. `*p+i`", "C. `(*p)+i+1`", "D. `&(p+i)`"], "A", "数组下标语法本质上是指针偏移后解引用。", ["高频", "指针", "数组"], "easy"), "指针"),
        _tag(single_choice("L2C-154", "在顺序查找长度为 n 的数组时，最坏情况下比较次数通常为：", ["A. 1", "B. log2 n", "C. n", "D. n^2"], "C", "目标不存在或在末尾时需要比较 n 次。", ["高频", "算法", "查找"], "easy"), "算法与数据结构"),
        _tag(single_choice("L2C-155", "若函数需要修改主调字符串内容，较合理的形参定义方式是：", ["A. `char s`", "B. `char *s`", "C. `int s[]`", "D. `double *s`"], "B", "字符串修改通常通过字符指针或字符数组参数完成。", ["高频", "函数", "字符串"], "easy"), "函数"),
        _tag(single_choice("L2C-156", "下列关于单链表的叙述中，正确的是：", ["A. 任意结点都能 O(1) 找到前驱", "B. 结点不必在内存中连续存放", "C. 插入删除一定比顺序表慢", "D. 不能遍历"], "B", "链表结点通过指针链接，无需连续存储。", ["高频", "结构体", "链表"], "easy"), "结构体"),
        _tag(single_choice("L2C-157", "若 `FILE *fp = fopen(\"a.dat\", \"rb\");`，其中 `b` 的含义是：", ["A. 追加", "B. 二进制方式", "C. 带缓冲", "D. 只读目录"], "B", "rb 表示只读打开二进制文件。", ["高频", "文件", "二进制"], "easy"), "文件"),
        _tag(single_choice("L2C-158", "若 `int *p = (int*)malloc(sizeof(int)*n);` 后未判断 p 是否为 NULL，主要风险是：", ["A. 程序变快", "B. 申请失败时后续解引用可能崩溃", "C. n 会自动变 0", "D. 内存一定会自动补 0"], "B", "申请失败后返回 NULL，继续使用会造成严重错误。", ["高频", "动态内存", "malloc"], "easy"), "动态内存"),
        _tag(single_choice("L2C-159", "关于 `switch` 语句中的 `default` 分支，下列说法正确的是：", ["A. 必须出现在第一个 case 之前", "B. 可以省略", "C. 只能写在最后一行", "D. 每个 switch 必须写两个 default"], "B", "default 是可选分支，只在没有任何 case 匹配时执行。", ["高频", "控制结构", "switch"], "easy"), "控制结构"),
        _tag(single_choice("L2C-160", "若要统计一个班级学生结构体数组中成绩不及格人数，核心是：", ["A. 只定义结构体不遍历", "B. 遍历数组并判断 score 是否小于及格线", "C. 用 printf 自动完成", "D. 只能用递归"], "B", "结构体数组统计题本质上仍是遍历 + 条件判断。", ["高频", "结构体", "程序设计"], "easy"), "程序设计"),

        _tag(fill_blank("L2C-161", "若要判断文件是否打开成功，`fopen` 的返回值应与 ______ 比较。", ["NULL"], "文件操作常见的第一步就是判空。", ["高频", "文件", "基础"], "easy"), "文件"),
        _tag(fill_blank("L2C-162", "将字符串按字符数组方式逆序时，常见做法是使用两个下标分别指向串首和串 ______。", ["尾", "末尾"], "首尾对称交换是经典高频题。", ["高频", "字符串", "算法"], "easy"), "字符串"),
        _tag(fill_blank("L2C-163", "若 `head` 是链表头指针，判断链表为空的常见条件是 `head == ______`。", ["NULL"], "空链表通常用空指针表示。", ["高频", "结构体", "链表"], "easy"), "结构体"),
        _tag(fill_blank("L2C-164", "函数参数传递中，若要让被调函数修改主调变量本身，通常需要传入该变量的 ______。", ["地址", "指针"], "值传递无法直接修改主调变量本身。", ["高频", "函数", "参数"], "easy"), "函数"),
        _tag(fill_blank("L2C-165", "在二维数组 `a[m][n]` 中，若要求第 i 行元素之和，需要再配合一个用于遍历 ______ 的循环。", ["列", "列下标"], "二维数组按行求和常用双层循环。", ["高频", "数组", "二维数组"], "easy"), "数组"),
        _tag(fill_blank("L2C-166", "若程序通过 `calloc` 申请空间，相比 `malloc` 更常见的附加特征是申请后空间会被初始化为 ______。", ["0", "零"], "calloc 常见考点就是清零。", ["高频", "动态内存", "calloc"], "medium"), "动态内存"),

        _tag(code_completion("L2C-167", "补全代码：`while(fgets(s, sizeof(s), fp) != NULL){ total += ______; }`，若每行是一个整数，应填：", ["atoi(s)", "strtol(s, NULL, 10)"], "文件逐行读入再转整数是常见模式。", ["高频", "文件", "程序设计"], "medium"), "文件"),
        _tag(code_completion("L2C-168", "补全代码：`for(i=0; i<n-1; i++) for(j=0; j<n-1-i; j++) if(a[j] > a[j+1]) { t=a[j]; a[j]=a[j+1]; a[j+1]=____; }`", ["t"], "冒泡排序的核心是相邻元素交换。", ["高频", "算法", "排序"], "easy"), "算法与数据结构"),
        _tag(code_completion("L2C-169", "补全代码：`struct node *p = head; while(p != NULL && p->data != x) p = p->____;`", ["next"], "链表查找常见写法。", ["高频", "结构体", "链表"], "easy"), "结构体"),
        _tag(code_completion("L2C-170", "补全代码：`char *p = s; while(*p != '\\0') { if(*p >= 'A' && *p <= 'Z') *p = *p + ____; p++; }`", ["32"], "大写转小写常利用 ASCII 差值。", ["高频", "字符串", "字符处理"], "easy"), "字符串"),
        _tag(code_completion("L2C-171", "补全代码：`int avg(int a[], int n){ int i,sum=0; for(i=0;i<n;i++) sum += a[i]; return sum / ______; }`", ["n"], "求平均值的基础模板。", ["高频", "数组", "函数"], "easy"), "数组"),
        _tag(code_completion("L2C-172", "补全代码：`if((p = (int*)malloc(sizeof(int)*n)) == NULL){ printf(\"memory error\\n\"); return ______; }`，设函数返回 int。", ["0", "-1"], "动态内存失败时应立刻返回错误值。", ["高频", "动态内存", "错误处理"], "medium"), "动态内存"),

        _tag(subjective("L2C-173", "bug_fix", "某程序写成 `char *p; gets(p);`，请指出这里的两个主要问题。", "一是 p 未指向有效缓冲区，二是 gets 无长度检查，存在严重溢出风险。", [rubric_item("指出 p 未指向有效空间", ["未指向", "未分配"], 4), rubric_item("指出 gets 不安全", ["gets", "不安全", "溢出"], 4), rubric_item("给出 fgets 或数组缓冲区修正思路", ["fgets", "数组", "缓冲区"], 2)], ["高频", "字符串", "安全"], "hard"), "字符串"),
        _tag(subjective("L2C-174", "bug_fix", "某程序删除链表结点时只执行了 `p = p->next;`，却没有修改前驱结点指针。请说明问题。", "只移动当前指针并不能真正把目标结点从链表中摘除，链表结构未被修改。", [rubric_item("指出未修改前驱 next", ["前驱", "next"], 4), rubric_item("指出链表结构未真正删除结点", ["未删除", "结构未改"], 4), rubric_item("可补充释放目标结点", ["free", "释放"], 2)], ["高频", "结构体", "链表"], "hard"), "结构体"),
        _tag(subjective("L2C-175", "bug_fix", "某程序计算平均值时写成 `avg = sum / n;`，其中 sum 和 n 都是 int。若结果需要保留小数，请说明问题。", "整型相除会丢失小数部分，应在运算时做类型转换。", [rubric_item("指出整型相除问题", ["整型相除", "小数丢失"], 4), rubric_item("指出需要类型转换", ["类型转换", "double", "float"], 4), rubric_item("说明输出格式", ["%.2f", "格式"], 2)], ["高频", "C基础", "bug_fix"], "medium"), "C基础"),
        _tag(subjective("L2C-176", "bug_fix", "某函数中使用 `free(p); free(p);`，请说明为什么这是危险的。", "重复释放同一块内存属于错误操作，可能导致程序崩溃或未定义行为。", [rubric_item("指出重复释放", ["重复释放"], 5), rubric_item("指出未定义行为或崩溃风险", ["未定义行为", "崩溃"], 3), rubric_item("提出释放后置空习惯", ["置空", "NULL"], 2)], ["高频", "动态内存", "bug_fix"], "medium"), "动态内存"),

        _tag(subjective("L2C-177", "programming", "说明如何编写函数，删除数组中的所有负数并把剩余元素前移。", "应包含遍历原数组、判断是否保留、使用新下标写回、返回新长度。", [rubric_item("遍历原数组", ["遍历"], 2), rubric_item("判断负数并跳过", ["负数", "跳过"], 3), rubric_item("用新下标前移保留元素", ["前移", "新下标"], 3), rubric_item("返回新长度", ["新长度", "返回"], 2)], ["高频", "程序设计", "数组"], "hard"), "程序设计"),
        _tag(subjective("L2C-178", "programming", "说明实现“统计文本中每个字母出现次数”的基本思路。", "应说明逐字符读取、判断是否为字母、映射到计数数组下标、最终输出统计结果。", [rubric_item("逐字符遍历", ["逐字符", "遍历"], 2), rubric_item("判断字母范围", ["字母", "范围"], 3), rubric_item("映射到计数数组", ["计数数组", "下标"], 3), rubric_item("输出结果", ["输出"], 2)], ["高频", "程序设计", "字符串"], "hard"), "程序设计"),
        _tag(subjective("L2C-179", "programming", "说明如何从文件中读取若干学生记录到结构体数组，并按成绩降序输出。", "应包含打开文件、循环读入结构体字段、记录数量、排序、输出与关闭文件。", [rubric_item("打开文件并判空", ["fopen", "NULL"], 2), rubric_item("循环读取记录", ["读取", "循环"], 3), rubric_item("排序", ["排序", "降序"], 3), rubric_item("输出并关闭文件", ["输出", "fclose"], 2)], ["高频", "程序设计", "结构体"], "hard"), "程序设计"),
        _tag(subjective("L2C-180", "programming", "说明如何设计函数判断两个字符串是否互为逆序串。", "应包含比较长度、逐对比较一端与另一端对应字符、全部匹配则成立。", [rubric_item("先比较长度", ["长度"], 2), rubric_item("首尾对应比较", ["对应比较", "逆序"], 4), rubric_item("发现不等提前结束", ["不等", "结束"], 2), rubric_item("全部通过则返回真", ["返回真", "成立"], 2)], ["高频", "程序设计", "字符串"], "medium"), "程序设计"),
    ]
