from __future__ import annotations


def _tag(question, topic: str, frequency: str = "high") -> dict:
    question["topic"] = topic
    question["frequency"] = frequency
    question["source"] = "curated"
    return question


def build_level2_c_high_frequency_pack(single_choice, fill_blank, code_completion, subjective, rubric_item):
    questions = [
        _tag(single_choice("L2C-063", "若 `int i=2;`，则表达式 `i++ + ++i` 的求值在标准 C 中应如何看待？", ["A. 结果一定是 6", "B. 结果一定是 5", "C. 属于未定义行为，不应依赖结果", "D. 编译必然报错"], "C", "同一序列点之间多次修改同一对象且未定义求值顺序，属于未定义行为。", ["高频", "运算符", "自增自减"], "hard"), "运算符与求值"),
        _tag(single_choice("L2C-064", "设 `char s[] = {'a','b','c'};`，将其作为字符串使用时最主要的问题是：", ["A. 占用空间过大", "B. 没有结束符 `\\0`", "C. 不能放在栈中", "D. 不能参与输出"], "B", "字符数组若作为字符串使用，必须保证有结束符。", ["高频", "字符串", "数组"], "medium"), "字符串"),
        _tag(single_choice("L2C-065", "若函数定义为 `void swap(int a,int b)`，在函数体内交换 a 与 b 后，对主调函数中的实参影响是：", ["A. 一定交换成功", "B. 只有全局变量才会交换成功", "C. 对实参没有影响", "D. 编译失败"], "C", "普通参数传递是值传递，修改的是形参副本。", ["高频", "函数", "参数传递"], "easy"), "函数与参数"),
        _tag(single_choice("L2C-066", "对一维数组 `int a[10];`，表达式 `a` 与 `&a[0]` 在大多数表达式中的关系是：", ["A. 类型和含义都完全不同，且数值一定不同", "B. 数值上通常相同，都指向首元素起始地址", "C. `a` 指向整个数组末尾", "D. `&a[0]` 不能赋给指针"], "B", "二者数值通常相同，但语义与类型环境存在差异。", ["高频", "数组", "指针"], "medium"), "指针与数组"),
        _tag(single_choice("L2C-067", "下列关于字符串处理函数的说法中，正确的是：", ["A. `strcpy` 会自动检查目标空间是否足够", "B. `strcmp` 返回 1 表示相等", "C. `strlen` 统计长度时不包含 `\\0`", "D. `strcat` 不会修改原目标串"], "C", "strlen 返回字符个数，不计结束符。", ["高频", "字符串", "库函数"], "easy"), "字符串"),
        _tag(single_choice("L2C-068", "若 `int *p, a = 10; p = &a;`，则 `(*p)++` 执行后的结果是：", ["A. p 的地址加 1", "B. a 变为 11", "C. *p 仍为 10", "D. 编译错误"], "B", "括号保证先解引用，再对所指变量自增。", ["高频", "指针", "优先级"], "easy"), "指针"),
        _tag(single_choice("L2C-069", "若要使一个函数只能在当前源文件中使用，较常见的做法是把函数定义为：", ["A. `extern`", "B. `register`", "C. `static`", "D. `volatile`"], "C", "文件作用域的 static 函数具有内部链接属性。", ["高频", "函数", "static"], "medium"), "函数与作用域"),
        _tag(single_choice("L2C-070", "关于递归与循环的叙述，正确的是：", ["A. 递归一定比循环更快", "B. 递归不需要结束条件", "C. 递归本质上也是通过重复求解子问题完成任务", "D. 递归不能处理阶乘问题"], "C", "递归通过函数自调用分解问题，必须有边界条件。", ["高频", "递归", "算法"], "easy"), "递归"),
        _tag(single_choice("L2C-071", "执行 `fopen(\"a.txt\",\"a\")` 打开文件后，写入位置通常在：", ["A. 文件开头", "B. 文件中间随机位置", "C. 文件末尾", "D. 不可写"], "C", "追加方式会把写位置定位到文件尾。", ["高频", "文件", "fopen"], "easy"), "文件"),
        _tag(single_choice("L2C-072", "若定义 `struct node { int data; struct node *next; };`，则该结构常用于表示：", ["A. 顺序表", "B. 单链表结点", "C. 二维数组", "D. 队列头指针"], "B", "数据域加 next 指针是单链表结点的典型定义。", ["高频", "结构体", "链表"], "easy"), "结构体与链表"),
        _tag(single_choice("L2C-073", "关于 `scanf(\"%d\", a);` 的说法中，正确的是：", ["A. 当 a 为整型变量时写法正确", "B. 当 a 为整型变量时，应改为 `&a`", "C. 只有在 Linux 下才需要 `&a`", "D. 使用 `%d` 时不能读入整数"], "B", "scanf 读取普通整型变量时需要传地址。", ["高频", "输入输出", "scanf"], "easy"), "输入输出"),
        _tag(single_choice("L2C-074", "若 `char *p = \"abc\";`，则下列说法正确的是：", ["A. `p[0]='A'` 一定安全", "B. p 指向字符串常量起始地址，修改内容通常是不安全的", "C. p 不是指针变量", "D. 字符串常量没有结束符"], "B", "字符串常量通常存放在只读区域，不应通过指针修改。", ["高频", "字符串", "指针"], "medium"), "字符串"),
        _tag(single_choice("L2C-075", "若 `int a[5] = {1,2};`，则数组 a 其余未显式初始化的元素值通常为：", ["A. 随机值", "B. 1", "C. 0", "D. 编译失败"], "C", "部分初始化时，其余元素自动补 0。", ["高频", "数组", "初始化"], "easy"), "数组"),
        _tag(single_choice("L2C-076", "下列关于 `return` 语句的叙述中，正确的是：", ["A. `void` 函数中绝不能出现 `return;`", "B. `return` 只能出现在 main 函数中", "C. `return` 会结束当前函数执行", "D. `return` 只对递归函数有效"], "C", "return 用于返回调用处并结束当前函数。", ["高频", "函数", "return"], "easy"), "函数"),
        _tag(single_choice("L2C-077", "在冒泡排序中，一趟排序结束后可以确定的是：", ["A. 最小元素一定到最左端", "B. 最大元素一定到最右端", "C. 所有元素已经有序", "D. 中间元素位置都确定"], "B", "标准升序冒泡一趟后，当前最大值会沉到末尾。", ["高频", "算法", "排序"], "medium"), "算法与排序"),
        _tag(single_choice("L2C-078", "若 `int x = 0; if(x) printf(\"Y\"); else printf(\"N\");`，输出结果为：", ["A. Y", "B. N", "C. 0", "D. 不确定"], "B", "在 C 中 0 表示假，非 0 表示真。", ["高频", "控制结构", "条件表达式"], "easy"), "控制结构"),
        _tag(single_choice("L2C-079", "关于 `sizeof` 运算符的叙述，正确的是：", ["A. `sizeof` 的结果单位通常是字节", "B. `sizeof` 只能用于数组", "C. `sizeof` 会返回变量当前值", "D. `sizeof(char)` 一定是 8"], "A", "sizeof 返回对象或类型所占字节数。", ["高频", "类型", "sizeof"], "easy"), "类型与内存"),
        _tag(single_choice("L2C-080", "若函数声明为 `int f(void);`，其中 `void` 的含义是：", ["A. 返回值为空", "B. 参数个数不确定", "C. 函数无参数", "D. 函数不可调用"], "C", "`void` 写在参数表中表示该函数不接收参数。", ["高频", "函数", "原型"], "easy"), "函数"),
        _tag(single_choice("L2C-081", "关于文件结束判断，下列说法更稳妥的是：", ["A. 先 `feof` 再读文件", "B. 应依据读函数返回值判断是否读成功", "C. 文件永远没有结束标志", "D. 只要打开成功就能一直读"], "B", "常见正确方式是根据 fgets、fscanf、fread 等返回值判断。", ["高频", "文件", "读取"], "medium"), "文件"),
        _tag(single_choice("L2C-082", "若 `int *p = NULL;`，则下列操作中最危险的是：", ["A. 判断 `p == NULL`", "B. 给 p 重新赋地址", "C. 直接执行 `*p = 3`", "D. 打印 p 的值"], "C", "空指针解引用会导致严重错误。", ["高频", "指针", "空指针"], "easy"), "指针"),
        _tag(single_choice("L2C-083", "在函数 `int f(int n){ if(n<=1) return 1; return n*f(n-1); }` 中，f 实现的是：", ["A. 求和", "B. 阶乘", "C. 斐波那契", "D. 最大公约数"], "B", "该递归式是典型阶乘定义。", ["高频", "递归", "程序阅读"], "easy"), "递归"),
        _tag(single_choice("L2C-084", "若 `char s[20] = \"abc\"; strcat(s, \"de\");`，执行后字符串 s 为：", ["A. abc", "B. de", "C. abcde", "D. cde"], "C", "strcat 会把源串拼接到目标串后面。", ["高频", "字符串", "库函数"], "easy"), "字符串"),
        _tag(single_choice("L2C-085", "下列关于二维数组形参的说法中，正确的是：", ["A. 第二维长度通常需要在形参中给出", "B. 二维数组不能作为函数参数", "C. 传参时会复制整个二维数组", "D. 只能定义成 `int **`"], "A", "二维数组按数组参数传递时，除首维外其余维度通常应明确。", ["高频", "数组", "函数"], "hard"), "数组与函数"),
        _tag(single_choice("L2C-086", "若使用 `malloc` 申请动态内存，申请成功后更合理的配套操作是：", ["A. 不需要任何额外处理", "B. 结束使用后调用 `free`", "C. 结束使用后调用 `fclose`", "D. 必须改成静态变量"], "B", "动态内存应在不再使用时释放，避免泄漏。", ["高频", "动态内存", "malloc"], "medium"), "动态内存"),

        _tag(fill_blank("L2C-087", "若要把字符数组作为字符串处理，最后一个字符位置通常应保留给 ______。", ["\\0", "空字符", "字符串结束符"], "C 字符串需要结束符，题目高频考查越界和长度问题。", ["高频", "字符串", "结束符"], "easy"), "字符串"),
        _tag(fill_blank("L2C-088", "若要在函数中交换两个主调变量的值，形参通常应设计为两个 ______ 参数。", ["指针", "地址"], "交换实参需要通过地址间接修改主调变量。", ["高频", "函数", "参数传递"], "easy"), "函数与参数"),
        _tag(fill_blank("L2C-089", "在 C 语言中，数组下标的起始值通常是 ______。", ["0"], "数组从 0 开始是最常见基础考点。", ["高频", "数组", "下标"], "easy"), "数组"),
        _tag(fill_blank("L2C-090", "表达式 `*p++` 中，若不加括号，优先级决定其含义通常是先对指针 ______，再取原位置的值。", ["自增", "递增"], "后缀 ++ 优先级高于 *，表达式等价于 `*(p++)`。", ["高频", "指针", "优先级"], "hard"), "指针"),
        _tag(fill_blank("L2C-091", "文本文件写入常用函数 `fprintf` 的第一个参数应是文件 ______。", ["指针", "文件指针", "FILE指针"], "fprintf 需要通过文件指针定位目标文件。", ["高频", "文件", "fprintf"], "easy"), "文件"),
        _tag(fill_blank("L2C-092", "若 `struct student s;`，则访问其成员 age 的表达式为 `s.______`。", ["age"], "结构体变量使用点运算符访问成员。", ["高频", "结构体", "成员访问"], "easy"), "结构体"),
        _tag(fill_blank("L2C-093", "递归函数中用于阻止无限调用、让递归能够结束的条件通常称为 ______ 条件。", ["边界", "终止", "结束"], "递归题几乎都会考到边界条件。", ["高频", "递归", "边界"], "easy"), "递归"),
        _tag(fill_blank("L2C-094", "若用 `fopen` 打开文件失败，返回值通常是 ______。", ["NULL"], "文件操作基础题高频考查空指针判断。", ["高频", "文件", "错误处理"], "easy"), "文件"),

        _tag(code_completion("L2C-095", "补全代码：`for(i=0; s[i] != '\\0'; i++) ;` 循环结束后，i 中保存的是字符串的 ______。", ["长度", "len"], "该模式是手写 strlen 的典型高频题。", ["高频", "字符串", "循环"], "easy"), "字符串"),
        _tag(code_completion("L2C-096", "补全代码：`void swap(int *a, int *b){ int t=*a; *a=*b; *b=____; }`", ["t"], "交换两个变量的经典指针题。", ["高频", "函数", "指针"], "easy"), "函数与参数"),
        _tag(code_completion("L2C-097", "补全代码：`if(n <= 1) return 1; else return n * f(____);`，使递归函数实现阶乘。", ["n-1"], "阶乘递归是最常见递归模板之一。", ["高频", "递归", "程序设计"], "easy"), "递归"),
        _tag(code_completion("L2C-098", "补全代码：`FILE *fp = fopen(\"data.txt\", \"r\"); if(fp == NULL) return ____;`，设函数返回 int。", ["0", "-1"], "文件打开失败时应立即返回错误值。", ["高频", "文件", "错误处理"], "medium"), "文件"),
        _tag(code_completion("L2C-099", "补全代码：`struct node *p = head; while(p != NULL){ p = p->____; }`", ["next"], "链表遍历题高频考查 next 指针。", ["高频", "结构体", "链表"], "easy"), "结构体与链表"),
        _tag(code_completion("L2C-100", "补全代码：`int sum(int a[], int n){ int i,s=0; for(i=0;i<n;i++) s += a[____]; return s; }`", ["i"], "数组遍历求和是最基础的程序设计题型。", ["高频", "数组", "循环"], "easy"), "数组"),
        _tag(code_completion("L2C-101", "补全代码：`char *p = s; while(*p) p++;` 该循环结束时，p 指向字符串的 ______。", ["结束符", "\\0"], "指针方式求串长常考。", ["高频", "字符串", "指针"], "medium"), "字符串"),
        _tag(code_completion("L2C-102", "补全代码：`int *p = (int*)malloc(sizeof(int) * n); if(p == NULL) { printf(\"fail\\n\"); return ____; }`，设函数返回 int。", ["0", "-1"], "动态内存题需要先判空。", ["高频", "动态内存", "malloc"], "medium"), "动态内存"),

        _tag(subjective("L2C-103", "bug_fix", "有代码 `char s[4] = \"abcd\";`，请指出问题并说明正确处理思路。", "字符串常量 \"abcd\" 需要 5 个字符位置（含结束符），长度为 4 的数组不足。", [rubric_item("指出结束符导致空间不足", ["结束符", "\\0", "空间不足"], 4), rubric_item("指出会发生越界或初始化不合法", ["越界", "不合法"], 3), rubric_item("给出修正方向", ["增大数组", "char s[5]"], 3)], ["高频", "字符串", "数组越界"], "medium"), "字符串"),
        _tag(subjective("L2C-104", "bug_fix", "某程序写成 `while(!feof(fp)){ fscanf(fp, \"%d\", &x); ... }`。请说明这种写法常见的问题。", "feof 往往在读取失败后才会置位，正确做法通常是直接判断 fscanf 等读函数返回值。", [rubric_item("指出 feof 判断滞后", ["滞后", "读取失败后"], 4), rubric_item("指出可能多处理一次无效数据", ["多读一次", "无效数据"], 3), rubric_item("给出以返回值判断的修正方向", ["返回值", "fscanf"], 3)], ["高频", "文件", "读取"], "hard"), "文件"),
        _tag(subjective("L2C-105", "bug_fix", "某函数中写有 `int *p; *p = 100;`，请说明错误原因。", "指针 p 未初始化就被解引用，会访问不确定地址。", [rubric_item("指出指针未初始化", ["未初始化", "随机地址"], 4), rubric_item("指出解引用危险", ["解引用", "非法访问"], 3), rubric_item("给出修正方向", ["赋有效地址", "malloc"], 3)], ["高频", "指针", "常见错误"], "easy"), "指针"),
        _tag(subjective("L2C-106", "bug_fix", "某程序把字符串比较写成 `if(s1 == s2)`，请说明这种写法在比较字符串内容时的问题。", "对字符数组或字符串指针直接用 == 比较，比较的是地址而非内容，应使用 strcmp。", [rubric_item("指出比较的是地址", ["地址", "不是内容"], 4), rubric_item("指出应比较内容", ["内容"], 2), rubric_item("给出 strcmp 修正方案", ["strcmp"], 4)], ["高频", "字符串", "比较"], "medium"), "字符串"),
        _tag(subjective("L2C-107", "programming", "说明编写函数统计数组中偶数个数的核心步骤。", "应包含遍历数组、判断偶数、计数累加、返回结果等关键点。", [rubric_item("遍历数组", ["遍历", "for"], 2), rubric_item("判断偶数", ["偶数", "%2"], 3), rubric_item("计数累加", ["计数", "++"], 3), rubric_item("返回结果", ["返回"], 2)], ["高频", "程序设计", "数组"], "easy"), "程序设计"),
        _tag(subjective("L2C-108", "programming", "说明设计一个函数，把字符串中的大写字母转换为小写字母的实现思路。", "应包含逐字符遍历、判断是否为大写字母、按编码规律转换、保持其他字符不变。", [rubric_item("逐字符遍历", ["遍历", "逐字符"], 2), rubric_item("判断大写字母范围", ["A", "Z", "大写"], 3), rubric_item("完成转换", ["转换", "+32", "差值"], 3), rubric_item("其他字符保持不变", ["不变"], 2)], ["高频", "程序设计", "字符串"], "medium"), "程序设计"),
        _tag(subjective("L2C-109", "programming", "给定学生结构体数组，说明如何按成绩从高到低排序。", "应说明遍历比较、交换结构体记录或交换索引、外层内层循环等思路。", [rubric_item("双层循环或等效排序流程", ["双层循环", "排序"], 3), rubric_item("比较成绩字段", ["成绩", "score"], 3), rubric_item("交换记录或索引", ["交换", "结构体"], 2), rubric_item("最终得到降序", ["降序", "从高到低"], 2)], ["高频", "程序设计", "结构体"], "medium"), "程序设计"),
        _tag(subjective("L2C-110", "programming", "说明实现“把文本文件中的整数逐个读出并求和”的主要步骤。", "应包含打开文件、循环读取、累加、处理失败与关闭文件。", [rubric_item("打开文件并判空", ["fopen", "NULL"], 3), rubric_item("循环读取整数", ["fscanf", "读取"], 3), rubric_item("累加求和", ["求和", "累加"], 2), rubric_item("关闭文件", ["fclose"], 2)], ["高频", "程序设计", "文件"], "medium"), "程序设计"),
    ]
    return questions
