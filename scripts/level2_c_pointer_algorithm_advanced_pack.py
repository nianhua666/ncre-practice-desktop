from __future__ import annotations


def _tag(question, topic: str, frequency: str = "high") -> dict:
    question["topic"] = topic
    question["frequency"] = frequency
    question["source"] = "curated"
    return question


def build_level2_c_pointer_algorithm_advanced_pack(single_choice, fill_blank, code_completion, subjective, rubric_item):
    return [
        _tag(single_choice("L2C-271", "若 `int *p = a;`，则遍历数组时写 `*(p+i)` 的本质是：", ["A. 访问第 i 个元素", "B. 删除第 i 个元素", "C. 创建新数组", "D. 访问数组长度"], "A", "指针偏移加解引用等价于数组下标访问。", ["高频", "指针", "数组"], "easy"), "指针"),
        _tag(single_choice("L2C-272", "若要在数组中查找某元素首次出现的位置，最直接的算法通常是：", ["A. 顺序查找", "B. 快速排序", "C. 归并排序", "D. 哈夫曼编码"], "A", "未说明有序时，顺序查找是最直接方案。", ["高频", "算法", "查找"], "easy"), "算法与数据结构"),
        _tag(single_choice("L2C-273", "在编写插入排序时，第 i 轮更核心的操作是：", ["A. 删除前 i 个元素", "B. 将第 i 个元素插入前面已排序序列的适当位置", "C. 整体逆序", "D. 重建数组"], "B", "插入排序的本质是维护前缀有序。", ["高频", "算法", "排序"], "medium"), "算法与数据结构"),
        _tag(single_choice("L2C-274", "若 `int **pp` 指向一个整型指针 p，则 `*pp` 的含义通常是：", ["A. 一个整型值", "B. p 所指向的整型值", "C. 指针 p 本身", "D. 数组长度"], "C", "二级指针解引用一次得到一级指针。", ["高频", "指针", "二级指针"], "medium"), "指针"),
        _tag(single_choice("L2C-275", "若要统计数组中不同元素的个数，较合理的做法通常是：", ["A. 直接返回数组长度", "B. 先排序再统计相邻不同值个数，或使用辅助结构", "C. 只比较前两个元素", "D. 一定要写入文件"], "B", "统计不同元素通常需要去重思路。", ["高频", "算法", "数组"], "hard"), "算法与数据结构"),
        _tag(single_choice("L2C-276", "对指针变量执行 `p++` 时，地址增加的字节数通常与什么有关？", ["A. 当前数组长度", "B. 所指向类型大小", "C. 文件长度", "D. 字符串内容"], "B", "指针算术以所指类型大小为步长。", ["高频", "指针", "类型"], "easy"), "指针"),
        _tag(single_choice("L2C-277", "若函数的形参是 `int a[]`，则在函数体中 `sizeof(a)` 的结果通常表示：", ["A. 整个实参数组大小", "B. 形参数组元素个数", "C. 指针大小", "D. 一定等于 0"], "C", "数组形参会退化为指针。", ["高频", "函数", "数组"], "hard"), "函数"),
        _tag(single_choice("L2C-278", "若要实现两个有序数组合并，较高效的核心思路通常是：", ["A. 双指针同步推进", "B. 把一个数组全部删除", "C. 只取最大值", "D. 每次都从头重新比较"], "A", "合并有序数组的常见线性做法是双指针。", ["高频", "算法", "数组"], "medium"), "算法与数据结构"),
        _tag(single_choice("L2C-279", "若一个指针已经被 `free` 释放，其后最安全的习惯是：", ["A. 立刻再次解引用", "B. 置为 NULL", "C. 指向任意整数", "D. 写入文件"], "B", "释放后置空可以降低误用风险。", ["高频", "动态内存", "指针"], "easy"), "动态内存"),
        _tag(single_choice("L2C-280", "若要对数组做原地逆序，较常见的空间复杂度是：", ["A. O(n)", "B. O(log n)", "C. O(1)", "D. O(n^2)"], "C", "只需有限几个辅助变量即可完成原地交换。", ["高频", "算法", "数组"], "medium"), "算法与数据结构"),

        _tag(fill_blank("L2C-281", "若要用指针遍历数组，循环中常通过 `p++` 让指针移向下一个 ______。", ["元素"], "指针算术在数组遍历中是高频考点。", ["高频", "指针", "数组"], "easy"), "指针"),
        _tag(fill_blank("L2C-282", "若要实现二分查找，除目标值和数组外，通常还需要维护左边界和右边界两个 ______。", ["下标", "索引"], "边界收缩是二分查找的核心。", ["高频", "算法", "查找"], "easy"), "算法与数据结构"),
        _tag(fill_blank("L2C-283", "若 `pp` 是二级指针，则表达式 `**pp` 在类型匹配时通常得到最终的 ______。", ["值", "整型值", "对象值"], "二级指针二次解引用后得到底层对象值。", ["高频", "指针", "二级指针"], "easy"), "指针"),
        _tag(fill_blank("L2C-284", "把数组中元素循环右移一位时，通常应先保存原数组最后一个 ______。", ["元素"], "循环右移会覆盖尾部信息，因此需先暂存。", ["高频", "数组", "程序设计"], "easy"), "数组"),
        _tag(fill_blank("L2C-285", "若函数返回动态分配的整型数组首地址，其返回类型更合理地写成 `int ______`。", ["*"], "动态数组返回常用指针类型。", ["高频", "函数", "动态内存"], "easy"), "函数"),
        _tag(fill_blank("L2C-286", "若要对一组整数按从小到大排序，比较交换类算法每次通常要比较两个 ______。", ["元素"], "排序类程序设计的基础。", ["高频", "算法", "排序"], "easy"), "算法与数据结构"),

        _tag(code_completion("L2C-287", "补全代码：`for(i=0;i<n-1;i++) for(j=0;j<n-1-i;j++) if(a[j] > a[j+1]) { t=a[j]; a[j]=a[j+1]; a[j+1]=____; }`", ["t"], "冒泡排序模板高频出现。", ["高频", "算法", "排序"], "easy"), "算法与数据结构"),
        _tag(code_completion("L2C-288", "补全代码：`int *p = a; for(i=0;i<n;i++) sum += *(p+____);`", ["i"], "指针偏移访问数组元素的基础模型。", ["高频", "指针", "数组"], "easy"), "指针"),
        _tag(code_completion("L2C-289", "补全代码：`mid = (low + high) / 2; if(key < a[mid]) high = mid - 1; else low = ______;`", ["mid + 1"], "二分查找边界更新模板。", ["高频", "算法", "查找"], "medium"), "算法与数据结构"),
        _tag(code_completion("L2C-290", "补全代码：`if(*p > max){ max = *p; pos = p - ______; }`，设 p 指向数组元素。", ["a"], "指针减去数组首地址可得到下标。", ["高频", "指针", "数组"], "hard"), "指针"),
        _tag(code_completion("L2C-291", "补全代码：`for(i=1;i<n;i++){ key = a[i]; j = i-1; while(j>=0 && a[j] > key){ a[j+1] = a[j]; j--; } a[j+1] = ____; }`", ["key"], "插入排序核心是把 key 插入合适位置。", ["高频", "算法", "排序"], "medium"), "算法与数据结构"),
        _tag(code_completion("L2C-292", "补全代码：`p = (int*)malloc(sizeof(int)*n); if(p == NULL) return 0; for(i=0;i<n;i++) scanf(\"%d\", p + ____);`", ["i"], "通过指针加偏移读入动态数组元素。", ["高频", "动态内存", "指针"], "medium"), "动态内存"),

        _tag(subjective("L2C-293", "bug_fix", "某程序在二分查找循环中把 `low = mid;` 写成右移边界更新。请说明风险。", "若不写成 `mid+1`，当 low 与 mid 重合时可能无法推进边界，导致死循环。", [rubric_item("指出边界无法推进", ["边界", "无法推进"], 4), rubric_item("指出可能死循环", ["死循环"], 4), rubric_item("给出应为 mid+1 的修正", ["mid+1"], 2)], ["高频", "算法", "bug_fix"], "hard"), "算法与数据结构"),
        _tag(subjective("L2C-294", "bug_fix", "某函数返回了动态数组首地址，但主调函数使用完后从未释放。请说明问题。", "这会造成动态内存泄漏，长期运行会持续占用内存。", [rubric_item("指出内存泄漏", ["内存泄漏"], 5), rubric_item("指出主调函数也有释放责任", ["主调", "释放"], 3), rubric_item("提出 free 修正", ["free"], 2)], ["高频", "动态内存", "bug_fix"], "medium"), "动态内存"),
        _tag(subjective("L2C-295", "bug_fix", "某程序想交换两个指针指向的值，却写成了交换两个局部形参指针本身。请说明问题。", "仅交换形参指针副本不会改变主调数据，若目标是交换值应交换 `*a` 与 `*b`。", [rubric_item("指出只改了形参副本", ["形参副本"], 4), rubric_item("指出主调数据未变", ["主调", "未变"], 3), rubric_item("提出交换解引用值", ["*a", "*b"], 3)], ["高频", "指针", "bug_fix"], "medium"), "指针"),
        _tag(subjective("L2C-296", "bug_fix", "某程序把数组形参当成完整数组来求元素个数，写成 `n = sizeof(a) / sizeof(a[0]);`。请说明问题。", "函数形参中的数组退化为指针，sizeof(a) 得到的是指针大小而不是实参数组总大小。", [rubric_item("指出数组形参退化为指针", ["退化", "指针"], 5), rubric_item("指出 sizeof 结果不再是数组总大小", ["sizeof", "指针大小"], 3), rubric_item("提出显式传入长度", ["传入长度"], 2)], ["高频", "函数", "bug_fix"], "hard"), "函数"),

        _tag(subjective("L2C-297", "programming", "说明如何编写函数，合并两个有序数组到第三个数组。", "应包含两个下标同步比较、较小值先放入目标数组、最后处理剩余部分。", [rubric_item("设置两个下标或指针", ["两个下标", "指针"], 3), rubric_item("比较较小值先放入", ["比较", "较小"], 4), rubric_item("处理剩余元素", ["剩余"], 3)], ["高频", "程序设计", "数组"], "hard"), "程序设计"),
        _tag(subjective("L2C-298", "programming", "说明如何用二分查找在有序数组中查找目标值。", "应包含初始化 low/high、循环计算 mid、比较目标与中间值并收缩边界。", [rubric_item("初始化边界", ["low", "high"], 2), rubric_item("计算 mid", ["mid"], 2), rubric_item("根据比较更新边界", ["比较", "更新边界"], 4), rubric_item("找到或失败返回", ["返回"], 2)], ["高频", "程序设计", "查找"], "medium"), "程序设计"),
        _tag(subjective("L2C-299", "programming", "说明如何编写函数，将一个数组循环左移 k 位。", "应包含处理 k 与长度关系、按位移动或借助临时数组、最终得到左移结果。", [rubric_item("处理 k 与长度关系", ["k", "长度"], 2), rubric_item("执行移动操作", ["移动", "左移"], 4), rubric_item("保持元素相对次序", ["相对次序"], 2), rubric_item("得到最终结果", ["结果"], 2)], ["高频", "程序设计", "数组"], "hard"), "程序设计"),
        _tag(subjective("L2C-300", "programming", "说明如何编写函数，返回数组中最大元素及其下标。", "应包含初始化最大值和位置、遍历数组、发现更大值时同步更新值和下标。", [rubric_item("初始化最大值和位置", ["初始化", "位置"], 3), rubric_item("遍历数组", ["遍历"], 2), rubric_item("发现更大值时同步更新", ["更新", "下标"], 5)], ["高频", "程序设计", "数组"], "easy"), "程序设计"),
    ]
