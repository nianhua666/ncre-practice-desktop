from __future__ import annotations


def _tag(question, topic: str, frequency: str = "high") -> dict:
    question["topic"] = topic
    question["frequency"] = frequency
    question["source"] = "curated"
    return question


def build_level2_c_problem_solving_pack(single_choice, fill_blank, code_completion, subjective, rubric_item):
    return [
        _tag(single_choice("L2C-111", "若 `char s[] = \"abc\";`，则 `sizeof(s)` 的结果通常为：", ["A. 2", "B. 3", "C. 4", "D. 不确定"], "C", "字符数组保存字符串常量时会包含结束符，因此大小为 4。", ["高频", "字符串", "sizeof"], "medium"), "字符串"),
        _tag(single_choice("L2C-112", "若 `int a[5]; int *p = a;`，则表达式 `p + 2` 指向：", ["A. 第 1 个元素", "B. 第 2 个元素", "C. 第 3 个元素", "D. 数组末尾后一个位置"], "C", "指针加 2 表示越过两个同类型元素，指向第三个元素。", ["高频", "指针", "数组"], "easy"), "指针"),
        _tag(single_choice("L2C-113", "关于 `scanf(\"%c\", &ch)` 的说法中，正确的是：", ["A. 不能读取空格", "B. 可以读取包括空格和换行在内的单个字符", "C. 必须把 `%c` 改成 `%s`", "D. 不需要传地址"], "B", "%c 读取单个字符，不会自动跳过空白字符。", ["高频", "输入输出", "scanf"], "medium"), "输入输出"),
        _tag(single_choice("L2C-114", "若 `int i = 1; int a[3] = {0}; a[i++] = i;`，则该语句的结果在标准 C 中应看作：", ["A. 一定正确且结果固定", "B. 一定结果为 1", "C. 可能涉及未定义行为，不应依赖", "D. 编译必错"], "C", "同一表达式中既修改又读取 i，且求值顺序未定义，不应依赖结果。", ["高频", "运算符", "未定义行为"], "hard"), "控制结构"),
        _tag(single_choice("L2C-115", "在排序 10 个整数时，若只需要找到最大值，较合适的思路是：", ["A. 完整冒泡排序", "B. 只做一次线性扫描", "C. 先转成字符串再比较", "D. 一定要使用递归"], "B", "只找最大值不必整体排序，线性扫描即可。", ["高频", "算法", "数组"], "easy"), "算法与数据结构"),
        _tag(single_choice("L2C-116", "关于 `#define SQR(x) x*x` 的说法中，正确的是：", ["A. 对 `SQR(a+b)` 的展开总是正确", "B. 宏定义在预处理阶段做文本替换，需注意括号", "C. 宏与函数完全等价", "D. 宏定义不能带参数"], "B", "函数式宏需要写成 `((x)*(x))` 才更稳妥。", ["高频", "预处理", "宏"], "medium"), "预处理"),
        _tag(single_choice("L2C-117", "若 `struct student stu[30];`，则 `stu[i].score` 表示：", ["A. 第 i 个学生的结构体地址", "B. 第 i 个学生 score 成员的值", "C. 所有学生的 score 总和", "D. 第 i+1 个学生的成绩"], "B", "数组元素是结构体变量，点运算符访问其成员。", ["高频", "结构体", "数组"], "easy"), "结构体"),
        _tag(single_choice("L2C-118", "执行 `rewind(fp);` 的作用通常是：", ["A. 删除文件", "B. 把文件位置指针移回开头", "C. 关闭文件", "D. 刷新标准输出"], "B", "rewind 常用于重新从文件开始位置读写。", ["高频", "文件", "文件指针"], "easy"), "文件"),
        _tag(single_choice("L2C-119", "若一个函数需要返回多个计算结果，更常见的 C 语言做法是：", ["A. 定义多个 return", "B. 通过指针参数或结构体返回", "C. 依赖全局随机变量", "D. 使用两个函数名"], "B", "C 语言函数只能直接返回一个值，多个结果常通过地址参数或结构体封装。", ["高频", "函数", "参数传递"], "medium"), "函数"),
        _tag(single_choice("L2C-120", "下列关于 `calloc` 与 `malloc` 的说法中，正确的是：", ["A. calloc 申请后通常会把空间清零", "B. malloc 会自动释放内存", "C. calloc 不能申请数组空间", "D. 两者都不返回指针"], "A", "calloc 通常会按元素个数和大小申请并置零。", ["高频", "动态内存", "calloc"], "medium"), "动态内存"),
        _tag(single_choice("L2C-121", "若要删除单链表中某个结点，核心操作之一通常是：", ["A. 修改前驱结点的 next 指针", "B. 修改数组长度", "C. 调整二维数组列数", "D. 使用 printf"], "A", "单链表删除需要改变前驱的链接关系。", ["高频", "链表", "结构体"], "medium"), "结构体"),
        _tag(single_choice("L2C-122", "关于局部变量和全局变量，下列说法正确的是：", ["A. 局部变量在整个程序运行期都存在", "B. 全局变量的作用域只在一个函数内", "C. 局部变量通常在所在代码块结束后失效", "D. 二者都不能初始化"], "C", "普通局部变量作用域和生命周期都比全局变量短。", ["高频", "作用域", "变量"], "easy"), "函数"),

        _tag(fill_blank("L2C-123", "若要释放由 `malloc` 申请的空间，应调用函数 ______。", ["free"], "malloc/free 配对是动态内存高频考点。", ["高频", "动态内存", "free"], "easy"), "动态内存"),
        _tag(fill_blank("L2C-124", "若希望 `scanf` 读取一个字符串时限制最大长度，可在格式串中加入字段 ______。", ["宽度", "长度限制"], "例如 `%9s` 可以限制最多读入 9 个可见字符。", ["高频", "字符串", "安全输入"], "medium"), "字符串"),
        _tag(fill_blank("L2C-125", "冒泡排序中，相邻元素比较后若前者大于后者，就应进行 ______。", ["交换"], "排序程序设计常考交换操作。", ["高频", "算法", "排序"], "easy"), "算法与数据结构"),
        _tag(fill_blank("L2C-126", "若要在函数内部改变外部数组内容，可以直接传数组名，因为它在参数传递时通常会退化为首元素 ______。", ["地址", "指针"], "这是数组和指针联系的典型考点。", ["高频", "数组", "指针"], "medium"), "数组"),
        _tag(fill_blank("L2C-127", "读取文本文件中一行字符串的常用更安全函数是 ______。", ["fgets"], "相对 gets，fgets 可以限制读入长度。", ["高频", "文件", "字符串"], "easy"), "文件"),
        _tag(fill_blank("L2C-128", "若 `p` 是结构体指针，访问其成员 name 通常写作 `p->______`。", ["name"], "结构体指针访问成员用箭头运算符。", ["高频", "结构体", "指针"], "easy"), "结构体"),

        _tag(code_completion("L2C-129", "补全代码：`int count = 0; for(i=0;i<n;i++) if(a[i] % 2 == 0) ______;`", ["count++", "++count"], "数组统计题的核心是条件满足时累加计数。", ["高频", "数组", "程序设计"], "easy"), "程序设计"),
        _tag(code_completion("L2C-130", "补全代码：`char s[20]; fgets(s, ______, stdin);` 若数组长度为 20，应填写：", ["20"], "fgets 第二参数通常是缓冲区长度。", ["高频", "字符串", "输入"], "easy"), "字符串"),
        _tag(code_completion("L2C-131", "补全代码：`for(i=0,j=n-1; i<j; i++,j--){ t=s[i]; s[i]=s[j]; s[j]=____; }`", ["t"], "逆序字符串时常用临时变量交换。", ["高频", "字符串", "交换"], "easy"), "字符串"),
        _tag(code_completion("L2C-132", "补全代码：`struct student *p = stu; for(i=0;i<n;i++,p++) sum += p->____;`，已知成员为 score。", ["score"], "结构体数组与指针遍历是常考题。", ["高频", "结构体", "数组"], "medium"), "结构体"),
        _tag(code_completion("L2C-133", "补全代码：`if((fp = fopen(\"a.txt\", \"r\")) == NULL){ printf(\"open error\\n\"); return ____; }`，设函数返回 int。", ["0", "-1"], "文件打开失败的错误处理是高频考点。", ["高频", "文件", "错误处理"], "medium"), "文件"),
        _tag(code_completion("L2C-134", "补全代码：`int *p = (int*)calloc(n, sizeof(int)); if(p == NULL) return 0; ... ______(p);`", ["free"], "动态内存申请后应及时释放。", ["高频", "动态内存", "释放"], "medium"), "动态内存"),

        _tag(subjective("L2C-135", "bug_fix", "某程序在循环中不断执行 `p = (int*)malloc(sizeof(int));`，但结束前没有释放任何空间。请指出问题。", "这会造成动态内存泄漏，循环次数多时风险更大。", [rubric_item("指出内存泄漏", ["内存泄漏", "泄漏"], 5), rubric_item("指出循环中重复申请会放大问题", ["循环", "重复申请"], 2), rubric_item("给出 free 修正方向", ["free", "释放"], 3)], ["高频", "动态内存", "bug_fix"], "easy"), "动态内存"),
        _tag(subjective("L2C-136", "bug_fix", "某程序用 `char ch; scanf(\"%d\", &ch);` 读取字符。请说明问题所在。", "格式控制符与变量类型不匹配，读取字符应根据需求用 `%c` 或先读整型再转换。", [rubric_item("指出格式与类型不匹配", ["格式", "类型不匹配"], 4), rubric_item("指出 `%c` 才用于字符读取", ["%c", "字符"], 3), rubric_item("说明可能导致错误结果", ["错误结果", "读取异常"], 3)], ["高频", "输入输出", "bug_fix"], "medium"), "输入输出"),
        _tag(subjective("L2C-137", "bug_fix", "某函数返回指向局部变量的指针，例如 `int *f(){ int x=3; return &x; }`。请说明错误。", "局部变量离开函数后生命周期结束，返回其地址会形成悬空指针。", [rubric_item("指出局部变量生命周期结束", ["局部变量", "生命周期"], 4), rubric_item("指出形成悬空指针", ["悬空", "无效地址"], 4), rubric_item("给出修正方向", ["静态", "动态内存", "参数传出"], 2)], ["高频", "函数", "指针"], "hard"), "函数"),
        _tag(subjective("L2C-138", "bug_fix", "某程序把 `strcpy(dest, src);` 用在两个重叠区域上。请说明为什么这种做法不稳妥。", "源和目标区域重叠时，简单复制可能产生不可预期结果，应避免重叠或采用更稳妥方案。", [rubric_item("指出源目标重叠问题", ["重叠"], 4), rubric_item("指出结果可能不可预期", ["不可预期", "错误"], 3), rubric_item("提出避免重叠或改方案", ["避免", "稳妥"], 3)], ["高频", "字符串", "bug_fix"], "medium"), "字符串"),
        _tag(subjective("L2C-139", "bug_fix", "在删除单链表结点时，程序直接 `free(p); p = p->next;`。请说明问题。", "结点释放后再访问 `p->next` 属于使用已释放内存，应先保存后继指针。", [rubric_item("指出释放后再访问错误", ["释放后", "访问"], 5), rubric_item("指出属于已释放内存使用", ["已释放", "非法"], 3), rubric_item("给出先保存 next 的修正思路", ["先保存", "next"], 2)], ["高频", "链表", "bug_fix"], "hard"), "结构体"),
        _tag(subjective("L2C-140", "programming", "说明设计一个函数，统计字符串中数字字符个数的实现步骤。", "应包括逐字符遍历、判断字符是否在 '0' 到 '9' 范围、计数累加和返回结果。", [rubric_item("逐字符遍历", ["遍历", "字符"], 2), rubric_item("判断是否为数字", ["'0'", "'9'", "数字"], 4), rubric_item("计数累加", ["计数", "累加"], 2), rubric_item("返回结果", ["返回"], 2)], ["高频", "程序设计", "字符串"], "easy"), "程序设计"),
        _tag(subjective("L2C-141", "programming", "说明编写函数实现选择排序的主要步骤。", "应包括外层确定当前位置、内层寻找最小或最大元素、必要时交换。", [rubric_item("外层确定待放置位置", ["外层", "位置"], 3), rubric_item("内层寻找最值", ["内层", "最小", "最大"], 3), rubric_item("交换元素", ["交换"], 2), rubric_item("重复直到完成排序", ["重复", "排序完成"], 2)], ["高频", "程序设计", "排序"], "medium"), "算法与数据结构"),
        _tag(subjective("L2C-142", "programming", "说明如何编写函数，删除字符串尾部的换行符。", "应包括求长度或定位换行、判断是否存在换行符、替换为结束符。", [rubric_item("定位尾部换行", ["换行", "\\n"], 4), rubric_item("判断是否存在", ["判断", "存在"], 2), rubric_item("替换为结束符", ["\\0", "结束符"], 4)], ["高频", "程序设计", "字符串"], "medium"), "程序设计"),
        _tag(subjective("L2C-143", "programming", "给定若干整数保存在文件中，说明如何找出其中的最大值。", "应包括打开文件、读入第一个值初始化最大值、循环比较更新、关闭文件。", [rubric_item("打开文件并判空", ["fopen", "NULL"], 3), rubric_item("初始化最大值", ["初始化", "最大值"], 2), rubric_item("循环比较更新", ["循环", "更新"], 3), rubric_item("关闭文件", ["fclose"], 2)], ["高频", "程序设计", "文件"], "medium"), "程序设计"),
        _tag(subjective("L2C-144", "programming", "说明如何用链表存储若干整数并统计结点个数。", "应说明结点定义、创建结点、链接 next 指针、遍历计数。", [rubric_item("定义结点结构", ["结构体", "next"], 2), rubric_item("创建并链接结点", ["创建", "链接"], 4), rubric_item("遍历链表", ["遍历", "while"], 2), rubric_item("计数", ["计数"], 2)], ["高频", "程序设计", "链表"], "hard"), "结构体"),
        _tag(subjective("L2C-145", "programming", "说明实现二维数组按行求和的核心思路。", "应包括双层循环、外层控制行、内层累加每一行元素并输出结果。", [rubric_item("双层循环", ["双层循环"], 3), rubric_item("外层按行", ["行"], 2), rubric_item("内层累加", ["累加", "求和"], 3), rubric_item("输出每行结果", ["输出"], 2)], ["高频", "程序设计", "二维数组"], "medium"), "数组"),
        _tag(subjective("L2C-146", "programming", "说明设计一个函数，把两个有序数组合并到第三个数组中的实现步骤。", "应说明双指针比较、较小元素先放入目标数组、处理剩余元素。", [rubric_item("使用两个下标或指针", ["两个下标", "指针"], 3), rubric_item("比较并先放较小元素", ["比较", "较小"], 4), rubric_item("处理剩余元素", ["剩余"], 3)], ["高频", "程序设计", "数组"], "hard"), "程序设计"),
        _tag(subjective("L2C-147", "bug_fix", "某程序使用 `while(*p != '\\0') p++;` 统计长度，但没有先让 p 指向有效字符串。请说明问题。", "指针未指向有效字符串就进行遍历，会访问非法内存。", [rubric_item("指出指针未指向有效区域", ["未指向", "非法"], 4), rubric_item("指出会访问非法内存", ["非法内存"], 4), rubric_item("给出初始化指针修正方向", ["初始化", "指向字符串"], 2)], ["高频", "指针", "bug_fix"], "medium"), "指针"),
        _tag(subjective("L2C-148", "bug_fix", "某程序在读取文件后忘记调用 `fclose`。请说明潜在影响。", "文件资源未及时释放，可能导致数据未完整写入、句柄泄漏或后续操作异常。", [rubric_item("指出资源未释放", ["资源", "未释放"], 4), rubric_item("指出可能影响写入或句柄", ["写入", "句柄"], 3), rubric_item("提出 fclose 修正", ["fclose"], 3)], ["高频", "文件", "bug_fix"], "easy"), "文件"),
        _tag(subjective("L2C-149", "programming", "说明如何判断一个字符串是否是回文串。", "应包括计算长度、首尾双指针比较、出现不等即可结束。", [rubric_item("计算长度", ["长度"], 2), rubric_item("首尾比较", ["首尾", "比较"], 4), rubric_item("不等时结束", ["不等", "结束"], 2), rubric_item("全部通过则判定成功", ["成功", "回文"], 2)], ["高频", "程序设计", "字符串"], "medium"), "程序设计"),
        _tag(subjective("L2C-150", "programming", "说明如何编写函数，计算 n 个整数的平均值并保留两位小数输出。", "应包含累加求和、类型转换避免整除、除以 n、按格式输出。", [rubric_item("累加求和", ["求和", "累加"], 3), rubric_item("避免整除", ["类型转换", "整除"], 3), rubric_item("除以 n 求平均", ["平均", "除以"], 2), rubric_item("格式化输出", ["%.2f", "两位小数"], 2)], ["高频", "程序设计", "数组"], "easy"), "程序设计"),
    ]
